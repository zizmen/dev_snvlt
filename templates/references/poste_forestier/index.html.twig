{% extends 'template_base.html.twig' %}

{% block Title %}
    SNVLT - Administrateur
{% endblock %}

{% block titre_page %}
    <div class="">
        <img src="{{ asset('assets/images/webapp/poste_forestier.png') }}" height="64" alt="dr"> Postes Forestiers <a
                class="btn btn-sm fs-5 text-light" style="background-color: #f13306;float: right" href="{{ path('poste_forestier.edit') }}"><i class="bi bi-plus-circle-fill me-2"></i>Ajouter</a>
    </div>

{% endblock %}

 {% block menu %}
     {% include 'base/menu.html.twig' %}
 {% endblock %}

{% block page_content %}
    <link rel="stylesheet" href="{{ asset('assets/webapp/assets/vendors/simple-datatables/style.css') }}">

    <section class="section">
        <div class="card">
            <div class="card-header">
                <span class="fw-bold fs-4 p-2 mb-5" style="color: #3f5a70">Interface de gestion des Postes Forestiers</span> <p class="alert-secondary p-2 text-dark text-end rounded-4 text-danger"> cliquez sur le poste forestier dans la liste pour la modifier</p>
            </div>
            <div class="card-body">
                <table class="table" id="pfs">
                    <thead>
                    <tr style="background: #e2ffed">
                        <th>Dénomination</th>
                        <th>Responsable</th>
                        <th>Cantonnement</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for pf in liste_poste_forestiers %}

                        <tr>
                            <td class="text-dark fw-bold"><a class="link-dark editBtn" href="{{ path('poste_forestier.edit', {'id_poste_forestier': pf.id })}}" id="{{ pf.id }}">{{ pf.denomination }}</a></td>
                            <td>{{ pf.emailResponsable }}</td>
                            <td> <span class="badge bg-light text-secondary p-2">{{ pf.codeCantonnement.nomCantonnement}}</span> </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

    </section>

    <script src="{{ asset('assets/webapp/assets/vendors/simple-datatables/simple-datatables.js') }}"></script>
    <script src="{{ asset('assets/webapp/assets/vendors/jquery/jquery.min.js') }}"></script>

    <script>



        // Simple Datatable
        let table1 = document.querySelector('#pfs');
        let dataTable = new simpleDatatables.DataTable(table1);


    </script>
    <script src="{{ asset('assets/webapp/assets/js/main.js') }}"></script>


{% endblock %}

