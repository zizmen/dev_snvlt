{% extends 'template_base.html.twig' %}

{% block Title %}
    {% trans %}SNVLT{% endtrans %} - {% trans %}Manage Document Stock{% endtrans %}
{% endblock %}

{% block titre_page %}
    <img src="{{ asset('assets/images/webapp/stock.png') }}" alt="dr">   <a href="{{ path('ref_cantonnements') }}"> {% trans %}Manage Document Stock{% endtrans %}[<span class="text-danger"> {{ detail_stock.createdAt | date('d M Y  h:i')  }} | {{ detail_stock.codeTypeDocStat.abv }} </span> ]</a> {% trans %}SNVLT{% endtrans %} - {% trans %}Generated by{% endtrans %} {{  detail_stock.createdBy}}

{% endblock %}

 {% block menu %}
     {% include 'base/menu.html.twig' %}
 {% endblock %}

{% block page_content %}

    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />

    <section class="section bg-white p-3 container">
            <table class="table table-stripped">
                <thead class=" bg-success text-light">
                <tr style="">
                    <th>{% trans %}Document No{% endtrans %}</th>
                    <th>{% trans %}In use{% endtrans %}</th>
                </tr>
                </thead>
                <tbody>
                {% set foo = 0 %}
                {% for detail in details_doc %}
                    {% set foo = foo + 1 %}

                    <tr>

                    <td class="text-dark fw-bold"><a class="link-dark editBtn" href="{{ path('docgen.show', {'id_doc': detail.id })}}" id="{{ detail.id }}">{{ detail.numeroDoc }}</a></td>
                    <td>
                        {% if( detail.attribue) %}
                            <input type="checkbox" readonly="true" value="checked">
                        {% else %}
                            <input type="checkbox" readonly="true" value="unchecked">
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                    <span class="fw-bold  bg-light text-danger fp-1 m-2">{{ foo }} {% trans %}documents{% endtrans %}</span>
                </tfoot>
            </table>


        <!--Modification de l'dr -->

    </section>

    <script src="{{ asset('assets/webapp/assets/vendors/simple-datatables/simple-datatables.js') }}"></script>
    <script src="{{ asset('assets/webapp/assets/vendors/jquery/jquery.min.js') }}"></script>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-beta.1/dist/js/select2.min.js"></script>

    <script>
        // Simple Datatable
        let table1 = document.querySelector('#drs');
        let dataTable = new simpleDatatables.DataTable(table1);
    </script>
    <script>

        $(document).ready(function() {
            $('.codetype').select2({
                placeholder: "--Sélectionnez la direction départementale...",
                theme: "bootstrap-5"
            });

        });

    </script>
    <script src="{{ asset('assets/webapp/assets/js/main.js') }}"></script>


{% endblock %}